meta {
  name: Remove all template tags
  type: http
  seq: 3
}

put {
  url: {{ehrApiUrl}}/template/:templateId/tag?tags=[]
  body: none
  auth: inherit
}

params:query {
  tags: []
}

params:path {
  templateId: {{templateId}}
}
