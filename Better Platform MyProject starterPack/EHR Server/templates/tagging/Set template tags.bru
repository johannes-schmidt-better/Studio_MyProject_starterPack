meta {
  name: Set template tags
  type: http
  seq: 2
}

put {
  url: {{ehrApiUrl}}/template/:templateId/tag?tags=ward:icu|mobile:true
  body: none
  auth: inherit
}

params:query {
  tags: ward:icu|mobile:true
}

params:path {
  templateId: {{templateId}}
}
